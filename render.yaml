services:
  - type: web
    name: insurance-financing-quote-api
    runtime: docker
    rootDir: .
    dockerfilePath: ./backend/Dockerfile
    plan: free
    envVars:
      - key: QUOTE_ENGINE
        value: auto
